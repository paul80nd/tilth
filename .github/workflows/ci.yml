# Build + test gate for pull requests — notably Dependabot's dependency-bump PRs, so a bad bump
# is caught before it merges. Pushes to main are gated by the deploy workflow's own build+test.
name: CI

on:
  pull_request:
  workflow_dispatch:

# One run per branch; a newer push supersedes an in-flight run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build # tsc -b && vite build
      - run: npm test # vitest run (unit + feature + component)
